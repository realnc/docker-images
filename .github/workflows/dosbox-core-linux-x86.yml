name: DOSBox-core Linux x86

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'dosbox-core/linux/Dockerfile'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DOCKER_BUILDKIT: 1

    steps:
      - uses: actions/checkout@v2

      - name: Build
        run: |
          cd dosbox-core/linux
          docker build \
            --progress plain \
            -t realnc/dosbox-core-build:linux-x86 \
            --platform linux/amd64 \
            --build-arg FROM_ARCH=i386 \
            --build-arg SHELL_WRAPPER=/usr/bin/linux32 \
            .
          echo ${{ secrets.DOCKERHUB_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USER }} --password-stdin
          docker push realnc/dosbox-core-build:linux-x86
